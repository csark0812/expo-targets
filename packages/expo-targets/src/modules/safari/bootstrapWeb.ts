import type React from 'react';

/**
 * Native / non-document stub — real ReactDOM bootstrap lives in
 * `bootstrapWeb.web.ts` (Metro platform extension).
 */
export function mountSafariExtensionRoot(
  _Component: React.ComponentType<any>
): void {
  /* no-op outside web */
}
