# Extract / rename

v0 lives in this monorepo as `@expo-targets/devicewright` (private).

Extract when:

1. Week-7 (or later) host claim matrix is stable
2. A concrete **non–expo-targets** consumer exists

Then rename scope to `@devicewright/*` (or final brand) and publish. Split into `core` / `ios` / `android` / `mcp` / `test` packages at extract time if still collapsed.

Brand/name finalization is **out of scope** for v0.
