import Foundation
import UIKit

/// Minimal broadcast-upload stub for expo-targets example (BroadcastUpload).
@objc(SampleHandler)
class SampleHandler: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
