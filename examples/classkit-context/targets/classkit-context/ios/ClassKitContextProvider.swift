import Foundation
import UIKit

/// Minimal classkit-context stub for expo-targets example (ClasskitContext).
@objc(ClassKitContextProvider)
class ClassKitContextProvider: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
