# Native Share example

Swift `ShareViewController` (no RN entry). RN host reads `nativeShare:items` via `AppGroupStorage`.

## OS path

1. Safari → Share → Native Share → Save to App
2. Host shows shared items
