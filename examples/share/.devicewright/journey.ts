/** Thin entry — Devicewright journey for this example (Maestro-style). */
export { runShareActionJourney as runJourney } from '../../.devicewright/journeys/share';
export const exampleId = 'share' as const;
