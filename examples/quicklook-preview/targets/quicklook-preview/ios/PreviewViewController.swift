import Foundation
import UIKit

/// Minimal quicklook-preview stub for expo-targets example (QuicklookPreview).
@objc(PreviewViewController)
class PreviewViewController: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
