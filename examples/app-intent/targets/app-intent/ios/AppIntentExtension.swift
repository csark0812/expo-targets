import Foundation
import UIKit

/// Minimal app-intent stub for expo-targets example (AppIntent).
@objc(AppIntentExtension)
class AppIntentExtension: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
