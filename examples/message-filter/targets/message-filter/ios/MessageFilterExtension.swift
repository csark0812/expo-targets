import Foundation
import UIKit

/// Minimal message-filter stub for expo-targets example (MessageFilter).
@objc(MessageFilterExtension)
class MessageFilterExtension: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
