import Foundation
import UIKit

/// Minimal smart-card stub for expo-targets example (SmartCard).
@objc(Token)
class Token: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
