import Foundation
import UIKit

/// Minimal spotlight stub for expo-targets example (Spotlight).
@objc(IndexRequestHandler)
class IndexRequestHandler: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
