import Foundation
import UIKit

/// Minimal matter stub for expo-targets example (Matter).
@objc(MatterSupportExtension)
class MatterSupportExtension: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
