/**
 * Bump this, then:
 *   npx expo-targets export-extension-bundles && eas update --branch preview
 * Release share sheet should show the new label after host fetch + App Group sync.
 */
export const OTA_LABEL = 'ota-v1';
