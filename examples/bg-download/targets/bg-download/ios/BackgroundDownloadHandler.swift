import Foundation
import UIKit

/// Minimal bg-download stub for expo-targets example (BgDownload).
@objc(BackgroundDownloadHandler)
class BackgroundDownloadHandler: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
