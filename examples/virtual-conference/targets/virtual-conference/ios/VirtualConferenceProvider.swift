import Foundation
import UIKit

/// Minimal virtual-conference stub for expo-targets example (VirtualConference).
@objc(VirtualConferenceProvider)
class VirtualConferenceProvider: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
