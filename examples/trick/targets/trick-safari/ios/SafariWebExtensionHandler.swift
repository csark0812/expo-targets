import Foundation
import UIKit

/// Minimal safari stub for expo-targets example (Safari).
@objc(SafariWebExtensionHandler)
class SafariWebExtensionHandler: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
