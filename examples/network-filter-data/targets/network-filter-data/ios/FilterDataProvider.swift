import Foundation
import UIKit

/// Minimal network-filter-data stub for expo-targets example (NetworkFilterData).
@objc(FilterDataProvider)
class FilterDataProvider: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
