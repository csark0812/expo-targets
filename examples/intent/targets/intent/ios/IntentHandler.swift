import Foundation
import UIKit

/// Minimal intent stub for expo-targets example (Intent).
@objc(IntentHandler)
class IntentHandler: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
