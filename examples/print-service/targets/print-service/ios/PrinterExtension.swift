import Foundation
import UIKit

/// Minimal print-service stub for expo-targets example (PrintService).
@objc(PrinterExtension)
class PrinterExtension: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
