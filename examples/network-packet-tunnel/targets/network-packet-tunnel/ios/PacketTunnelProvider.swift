import Foundation
import UIKit

/// Minimal network-packet-tunnel stub for expo-targets example (NetworkPacketTunnel).
@objc(PacketTunnelProvider)
class PacketTunnelProvider: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
