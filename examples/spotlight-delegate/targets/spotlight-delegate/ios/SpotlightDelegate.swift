import Foundation
import UIKit

/// Minimal spotlight-delegate stub for expo-targets example (SpotlightDelegate).
@objc(SpotlightDelegate)
class SpotlightDelegate: NSObject {
  // Extension principal — replace with full Apple API conformance as needed.
}
